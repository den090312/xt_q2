@using WebPL.Models;
@using Entities;
@using Common;

@{
    Layout = "~/Pages/_Layout.cshtml";

    if (IsPost)
    {
        Index.Forms = Request.Form;
        var orderList = Index.GetOrderList();

        //POST-reload page fix
        @Html.Raw("<script>document.location='Orders.cshtml'</script>");
    }
}
